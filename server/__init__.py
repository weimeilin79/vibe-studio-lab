"""Vibe Studio server: one FastAPI process that serves the React frontend,
a JSON + SSE API over the lab's backend drivers, the platform (Wall) API the
pipeline publishes to, and the ADK dev UI mounted under /inspector."""
