"""The app's plumbing and platform clients: the event bus and SSE stream (bus),
the files the app keeps (files), publishing to vibetube.dev (publish), the
avatar job (avatar), Cloud Trace export (telemetry), the graph drawing
(graphinfo). main, api and runner import from here."""
