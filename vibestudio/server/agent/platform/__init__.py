"""The platform side of the agent: env and paths (config), the session store
helpers (drive), the run file (state), and the GEAP clients: Memory Bank
(memory, bank), RAG Engine (rag), Veo (videogen). The graph in agent/ imports
from here; nothing here imports the graph."""
